<settings>
    <servers>
        <server>
            <!-- The id here must match the repository id given in altDeploymentRepository -->
            <id>gitlab</id>
            <username>gitlab-ci-token</username>
            <password>${env.CI_JOB_TOKEN}</password>
        </server>
    </servers>
</settings>